class DistrictObject {
  int active, confirmed, deceased, recovered;
  String districtName, stateName;

  DistrictObject({
    this.active,
    this.confirmed,
    this.deceased,
    this.recovered,
    this.districtName,
    this.stateName,
  });
}
